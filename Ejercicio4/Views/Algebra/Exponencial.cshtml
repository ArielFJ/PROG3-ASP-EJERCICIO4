@model OperandosViewModel
@{
    ViewData["Title"] = "Neperiano";
}

<div class="mt-lg-5 ml-lg-5">

    <form asp-controller="Algebra" asp-action="Exponencial" method="post">

        <div class="form-row align-items-center">

            <div class="col-lg-7" style="margin-right:-7.5rem; margin-left:9rem">
                <div class="form-group">
                    <div class="col-md-5">
                        <label class="control-label">E elevado a </label>
                        <input asp-for="Num1" type="number" step="0.0000001"  class="form-control" />
                        <span asp-validation-for="Num1" class="text-danger"></span>
                    </div>
                </div>
            </div>

        </div>


        <div class="form-group" style="margin:5% 41%">
            <div class="col-md-5">
                <button type="submit" class="btn btn-danger">Calcular</button>
            </div>
        </div>

    </form>
</div>

@if (Model.Resultado != null)
{
    <div class="text-center">
        <h1>El resultado es @((double)Model.Resultado)</h1>
    </div>
}