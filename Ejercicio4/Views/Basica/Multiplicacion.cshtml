@model OperandosViewModel
@{
    ViewData["Title"] = "Multiplicación";
}

<div class="mt-lg-5 ml-lg-5">

    <p><b>La multiplicación es una operación binaria que se establece en un conjunto numérico.​ Tal el caso de números naturales, consiste en sumar un número tantas veces como indica otro número. Así, 4×3 es igual a sumar tres veces el valor 4 por sí mismo. Es una operación diferente de la adición, pero equivalente. </b></p>

    <form asp-controller="Basica" asp-action="Multiplicacion" method="post">

        <div class="form-row align-items-center">

            <div class="col-lg-5" style="margin-right:-7.5rem; margin-left:9rem">
                <div class="form-group">
                    <div class="col-md-5">
                        <label asp-for="Num1" class="control-label"></label>
                        <input asp-for="Num1" type="number" step="0.0001" class="form-control" />
                        <span asp-validation-for="Num1" class="text-danger"></span>
                    </div>
                </div>
            </div>

            <div class="col"><h1>x</h1></div>

            <div class="col-lg-5" style="margin-left:4rem">
                <div class="form-group">
                    <div class="col-md-5">
                        <label asp-for="Num2" class="control-label"></label>
                        <input asp-for="Num2" type="number" step="0.0001" class="form-control" />
                        <span asp-validation-for="Num2" class="text-danger"></span>
                    </div>
                </div>
            </div>

        </div>


        <div class="form-group" style="margin:5% 41%">
            <div class="col-md-5">
                <button type="submit" class="btn btn-danger">Calcular</button>
            </div>
        </div>

    </form>
</div>

@if (Model.Resultado != null)
{
    <div class="text-center">
        <h1>El resultado es @((double)Model.Resultado)</h1>
    </div>
}